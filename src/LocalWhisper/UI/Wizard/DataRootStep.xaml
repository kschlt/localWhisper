<UserControl x:Class="LocalWhisper.UI.Wizard.DataRootStep"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40,30">
        <TextBlock Text="Speicherort wählen"
                  FontSize="16"
                  FontWeight="SemiBold"
                  Margin="0,0,0,10"/>

        <TextBlock Text="Wählen Sie den Speicherort für Ihre Daten (Konfiguration, Modelle, Verlauf):"
                  TextWrapping="Wrap"
                  Margin="0,0,0,20"
                  Foreground="#666666"/>

        <Label Content="Datenordner:" Margin="0,0,0,5"/>

        <Grid Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="DataRootTextBox"
                    Grid.Column="0"
                    IsReadOnly="True"
                    Padding="8"
                    VerticalContentAlignment="Center"
                    Background="#F5F5F5"/>

            <Button x:Name="BrowseButton"
                   Grid.Column="1"
                   Content="Durchsuchen..."
                   Padding="15,8"
                   Margin="10,0,0,0"
                   Click="BrowseButton_Click"/>
        </Grid>

        <TextBlock x:Name="InfoText"
                  Text="Der Standardspeicherort wurde automatisch ausgewählt."
                  Foreground="#4CAF50"
                  FontStyle="Italic"
                  Margin="0,0,0,20"/>

        <Border BorderBrush="#E0E0E0" BorderThickness="1" Background="#FAFAFA" Padding="15" Margin="0,10,0,0">
            <StackPanel>
                <TextBlock Text="💡 Hinweis" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <TextBlock TextWrapping="Wrap" Foreground="#666666">
                    In diesem Ordner werden alle Daten gespeichert:<LineBreak/>
                    • Konfigurationsdateien<LineBreak/>
                    • Whisper-Modelle<LineBreak/>
                    • Diktat-Verlauf<LineBreak/>
                    • Protokolle
                </TextBlock>
            </StackPanel>
        </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
