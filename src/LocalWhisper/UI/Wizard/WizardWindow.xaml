<Window x:Class="LocalWhisper.UI.Wizard.WizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LocalWhisper - Ersteinrichtung"
        Width="600"
        Height="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="20,15">
            <StackPanel>
                <TextBlock Text="Willkommen bei LocalWhisper"
                          FontSize="18"
                          FontWeight="SemiBold"
                          Foreground="#333333"/>
                <TextBlock Text="Einrichtungsassistent"
                          FontSize="12"
                          Foreground="#666666"
                          Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Step Indicators -->
        <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="20,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <!-- Step 1 -->
                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <Ellipse x:Name="Step1Indicator"
                            Width="30"
                            Height="30"
                            Fill="#4CAF50"
                            Stroke="#4CAF50"
                            StrokeThickness="2"/>
                    <TextBlock Text="1. Datenordner"
                              VerticalAlignment="Center"
                              Margin="8,0,0,0"
                              FontWeight="SemiBold"/>
                </StackPanel>

                <!-- Arrow -->
                <TextBlock Text="→" FontSize="20" Margin="0,0,30,0" Foreground="#CCCCCC"/>

                <!-- Step 2 -->
                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <Ellipse x:Name="Step2Indicator"
                            Width="30"
                            Height="30"
                            Fill="Transparent"
                            Stroke="#CCCCCC"
                            StrokeThickness="2"/>
                    <TextBlock x:Name="Step2Text"
                              Text="2. Modell"
                              VerticalAlignment="Center"
                              Margin="8,0,0,0"
                              Foreground="#999999"/>
                </StackPanel>

                <!-- Arrow -->
                <TextBlock Text="→" FontSize="20" Margin="0,0,30,0" Foreground="#CCCCCC"/>

                <!-- Step 3 -->
                <StackPanel Orientation="Horizontal">
                    <Ellipse x:Name="Step3Indicator"
                            Width="30"
                            Height="30"
                            Fill="Transparent"
                            Stroke="#CCCCCC"
                            StrokeThickness="2"/>
                    <TextBlock x:Name="Step3Text"
                              Text="3. Hotkey"
                              VerticalAlignment="Center"
                              Margin="8,0,0,0"
                              Foreground="#999999"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <ContentControl x:Name="ContentArea"
                       Grid.Row="2"
                       Margin="0"/>

        <!-- Button Bar -->
        <Border Grid.Row="3" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="20,15" Background="#FAFAFA">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="CancelButton"
                       Grid.Column="0"
                       Content="Abbrechen"
                       Padding="15,8"
                       MinWidth="100"
                       Click="CancelButton_Click"/>

                <Button x:Name="BackButton"
                       Grid.Column="2"
                       Content="← Zurück"
                       Padding="15,8"
                       MinWidth="100"
                       Margin="0,0,10,0"
                       Click="BackButton_Click"
                       IsEnabled="False"/>

                <Button x:Name="NextButton"
                       Grid.Column="3"
                       Content="Weiter →"
                       Padding="15,8"
                       MinWidth="100"
                       Margin="0,0,10,0"
                       Click="NextButton_Click"/>

                <Button x:Name="FinishButton"
                       Grid.Column="4"
                       Content="Fertig"
                       Padding="15,8"
                       MinWidth="100"
                       Click="FinishButton_Click"
                       Visibility="Collapsed"
                       FontWeight="SemiBold"
                       Background="#4CAF50"
                       Foreground="White"/>
            </Grid>
        </Border>
    </Grid>
</Window>
